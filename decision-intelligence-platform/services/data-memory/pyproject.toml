[tool]
name = "data-memory"
version = "0.1.0"
description = "Data and memory management service for the decision intelligence platform."
authors = ["Your Name <your.email@example.com>"]
license = "MIT"

[dependencies]
fastapi = "^0.95.0"
sqlalchemy = "^2.0.0"
asyncpg = "^0.27.0"
redis = "^4.0.0"
pydantic = "^1.10.0"